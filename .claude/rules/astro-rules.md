---
description: Правила работы с Astro-компонентами и шаблонами
globs: ["src/**/*.astro", "src/**/*.ts"]
---

# Astro-правила

- Компоненты в `src/components/`, layouts в `src/layouts/`
- Страницы в `src/pages/`, контент в `src/content/blog/`
- TypeScript strict mode
- Не использовать React/Vue — только Astro-компоненты
- CSS-модули или глобальные стили в `src/styles/`
